# Expense Tracker API

A Django REST Framework project to manage daily expenses efficiently.

## Features
- User registration and authentication (Token-based)
- Add, update, delete expenses
- View expenses by date and category
- Get total expenses per day or category
- PostgreSQL database support

## Tech Stack
- Django
- Django REST Framework
- PostgreSQL
- Python-dotenv for environment variables

## Setup Instructions

1. **Clone the repository**
   ```bash
   git clone https://github.com/Midhungirish2002/expense-tracker-api.git
   cd expense-tracker-api
